<?xml version="1.0" encoding="utf-8"?>

<!--
====================================================================
File: app/src/main/AndroidManifest.xml
Version: 20.0.0

Batch 20:
  - Registers LIAS Remote navigation deep links.
  - Does NOT allow configuration/auth data through URI parameters.
  - Keeps normal launcher behavior unchanged.

Supported:

  liasremote://home
  liasremote://devices
  liasremote://device/<pdid>
  liasremote://devices/<pdid>
  liasremote://schedules
  liasremote://rules
  liasremote://settings
====================================================================
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".LiasApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="LIAS Remote"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiasRemote"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.LiasRemote"
            android:windowSoftInputMode="adjustResize">

            <!-- Normal launcher entry point. -->
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

            <!--
                LIAS Remote navigation links.

                We intentionally register only the custom scheme here.
                There is no token, server-address, or credential-bearing
                URI contract.
            -->
            <intent-filter>

                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="liasremote" />

            </intent-filter>

        </activity>

    </application>

</manifest>
