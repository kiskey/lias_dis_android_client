<?xml version="1.0" encoding="utf-8"?>

<!-- ==================================================================== -->
<!-- File: app/src/main/AndroidManifest.xml                               -->
<!-- Version: 3.0.0                                                       -->
<!--                                                                      -->
<!-- Purpose:                                                             -->
<!--   Application permissions and external LIAS navigation contract.     -->
<!--                                                                      -->
<!-- Deep links:                                                          -->
<!--   lias://device/device/<pdid>                                        -->
<!--   https://lias.local/device/<pdid>                                  -->
<!--                                                                      -->
<!-- The custom lias:// URI is the primary app-to-app contract.           -->
<!-- The HTTPS form provides a future web-compatible/shareable contract.  -->
<!-- ==================================================================== -->

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".LiasApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="LIAS Remote"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiasRemote"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.LiasRemote"
            android:windowSoftInputMode="adjustResize">

            <!-- Normal application launch -->
            <intent-filter>

                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

            <!--
                Primary LIAS application deep-link contract.

                Example:
                lias://device/device/pdid-123
            -->
            <intent-filter>

                <action
                    android:name="android.intent.action.VIEW" />

                <category
                    android:name="android.intent.category.DEFAULT" />

                <category
                    android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="lias"
                    android:host="device"
                    android:pathPrefix="/device" />

            </intent-filter>

            <!--
                Shareable HTTPS contract.

                Example:
                https://lias.local/device/pdid-123

                This is deliberately explicit rather than accepting every
                HTTPS URL associated with the application.
            -->
            <intent-filter>

                <action
                    android:name="android.intent.action.VIEW" />

                <category
                    android:name="android.intent.category.DEFAULT" />

                <category
                    android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="lias.local"
                    android:pathPrefix="/device" />

            </intent-filter>

        </activity>

    </application>

</manifest>
